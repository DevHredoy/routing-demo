
in this branch nested routing and indexing has been done .In nested routing there will be more nav other than the top nav of page in a particular
page. the url will also effected. that means the parent route is properly arranged under a parent route in url.

useSearch params:this hook is used to use filter. In websites we can filter search results.
this hook is used to modify the url.